# University Repository

Code submissions for Nazar Stepanenko, KPI group 4403.

## Structure

```
Year-2/Semester-2/
├── SSA/           # АСПЗ (Архітектура системного програмного забезпечення) — C
│   ├── LR/LR1..LR5   (completed)
│   └── HW/
├── Web/           # Основи Веб-програмування — HTML/CSS/JS/Node.js
│   ├── PR/PR1..PR6    (practicals)
│   ├── CW/            (coursework)
│   └── HW/
├── OOA/           # ООА та конструювання ПС — C#
│   └── LR/LR1..
└── SoftwareEngineering/  # Компоненти ПІ. Архітектура ПЗ
    └── PR/PR1..
```

## Task Descriptions

Full assignment text is in the Obsidian vault at:
`D:\Obsidian Projects\KPI\KPI\Tasks\<name>.md`

Content after the `---` separator contains the extracted teacher's document.

## Conventions

- **Commits:** conventional format — `feat(SSA/LR6): add signal handling lab`
- **File naming:** `task1.1/main.c` for sub-tasks, or `Pr1.js` for single-file tasks
- **Each LR/PR folder** should have source files and optionally a README.md
- **No Co-Authored-By** in commit messages
